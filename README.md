# GIT
Не забыть указать
```bash
git config --global user.name ...
git config --global user.email ...
```
Первый пуш в репозитории gh делать не даёт, даже с учётом синхронизации по SSH. Один раз ручками.

